const ProjectBtnPhoto = () => {
  //프로필 사진 불러오기
  return (
    <div
      style={{
        width: "60px",
        height: "60px",
        borderRadius: "50%",

        //이미지 삽입 후 삭제 예정 (사진 위치 보기 위해서 넣음)
        border: "1px solid var(--text-color1)",
        backgroundColor: "var(--line-color)",
      }}
    ></div>
  );
};

export default ProjectBtnPhoto;
